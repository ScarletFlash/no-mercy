/**
 * @description
 * It’s used to enable syntax highlighting for code inside text strings.
 * Prettier also picks it up to format that code according to the project’s rules.
 */
export const ts = String.raw;
